pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{ts,tsx,js,jsx,json,css}"
      run: bunx biome check --write {staged_files}
    type-check:
      run: bunx tsc --noEmit
pre-push:
  commands:
    test:
      run: bun run test
